{

    // function add(a, b){
    //     return a + b
    // }

    function add1(a: number = 100, b: number = 200): number{
        return a + b
    }

    // const sub = (a,b) => {
    //     return a - b
    // }
    const sub1 = (a: number=100,b:number=200) :number => {
        return a - b
    }
}